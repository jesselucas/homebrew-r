# homebrew-r
Homebrew formula for r : github.com/jesselucas/r

# Install
`brew tap jesselucas/r`
`brew install r`

# Update
`brew untap jesselucas/r`
`brew uinstall r`
`brew tap jesselucas/r`
`brew install r`
